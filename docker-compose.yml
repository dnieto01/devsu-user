services:
  redis:
    image: redis:7.0
    container_name: redis
    ports:
      - "6379:6379"

  app:
    build: .
    container_name: devsu-user-app
    ports:
      - "8095:8095"
    depends_on:
      - redis